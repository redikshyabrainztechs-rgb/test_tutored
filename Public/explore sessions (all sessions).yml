info:
  name: explore sessions (all sessions)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/public/list/sessions-cursor"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
