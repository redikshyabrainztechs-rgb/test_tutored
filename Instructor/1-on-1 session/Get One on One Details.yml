info:
  name: Get One on One Details
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/instructor/instructor-stats-for-dashboard"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
