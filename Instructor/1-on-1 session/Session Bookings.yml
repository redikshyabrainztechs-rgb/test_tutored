info:
  name: Session Bookings
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/instructor/get-one-on-one-details"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
