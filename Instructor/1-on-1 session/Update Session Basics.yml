info:
  name: Update Session Basics
  type: http
  seq: 8

http:
  method: PUT
  url: "{{baseUrl}}/instructor/availability"
  body:
    type: json
    data: '{"duration":60,"price":300,"currency":"NPR","description":"always on time"}'
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
