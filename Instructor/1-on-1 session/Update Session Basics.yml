info:
  name: Update Session Basics
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/instructor/availability"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
