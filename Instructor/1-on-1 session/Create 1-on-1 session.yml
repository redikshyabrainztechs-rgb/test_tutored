info:
  name: Create 1-on-1 session
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
