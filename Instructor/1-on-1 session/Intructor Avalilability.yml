info:
  name: Intructor Avalilability
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/instructor/instructor-stats-for-session"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
