info:
  name: Intructor Stats for Session
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/instructor/list/session-bookings"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
