info:
  name: Instructor Stats for session 1
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/instructor/instructor-stats-for-session"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
