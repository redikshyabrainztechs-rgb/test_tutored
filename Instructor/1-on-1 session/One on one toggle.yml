info:
  name: One on one toggle
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/instructor/update-basic-session"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
