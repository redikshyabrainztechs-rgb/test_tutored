info:
  name: Get languages spoken
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/group/language"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
