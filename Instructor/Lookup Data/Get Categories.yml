info:
  name: Get Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/group/category"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
