info:
  name: "Get Live Sessions "
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/instructor/list/live-sessions/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
