info:
  name: Students list
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/instructor/live-session/1df25548-95bc-4baa-a785-5a85afaf19f2/list/students"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
