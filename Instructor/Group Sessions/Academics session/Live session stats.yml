info:
  name: Live session stats
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/instructor/live-sessions/stats"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
