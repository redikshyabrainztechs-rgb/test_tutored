info:
  name: Group Data items
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/groups/data-ai/items"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
