info:
  name: Student by Course
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/instructor/list/student-by-course"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
