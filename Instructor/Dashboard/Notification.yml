info:
  name: Notification
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/users/notification"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
