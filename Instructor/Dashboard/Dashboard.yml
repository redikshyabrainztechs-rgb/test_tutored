info:
  name: Dashboard
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/instructor/live-sessions/list/dashboard"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
