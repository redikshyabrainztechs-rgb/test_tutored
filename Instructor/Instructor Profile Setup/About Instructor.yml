info:
  name: About Instructor
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/instructor/about-instructor"
  body:
    type: json
    data: |-
      {
      "city": "Kathmandu",
      "country": "Nepal",
      "detailed_bio": "detailed bio",
      "languages_spoken": ["b266cc5b-d826-495a-a388-45e0887006f2","b266cc5b-d826-495a-a388-45e0887006f2"],
      "short_bio": "short bio cookies and biscuits"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
