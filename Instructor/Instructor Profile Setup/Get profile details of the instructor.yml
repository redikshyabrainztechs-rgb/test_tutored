info:
  name: Get profile details of the instructor
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/instructor/get-profile-details"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
