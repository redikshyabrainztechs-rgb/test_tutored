info:
  name: Video Introduction
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/instructor/video-intro"
  body:
    type: json
    data: '{"video_url": ""}'
  auth:
    type: bearer
    token: "{{accessToekn}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
