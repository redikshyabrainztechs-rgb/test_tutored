info:
  name: Video Introduction
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/instructor/video-intro"
  body:
    type: json
    data: '{"video_url": "https://youtu.be/w0lH5rLG4xA?si=0YkWdSLceofY0hc5"}'
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
