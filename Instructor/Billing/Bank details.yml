info:
  name: Bank details
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/instructor/bank-details"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
