info:
  name: Instructor billing
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/instructor/list/instructor-billing"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
