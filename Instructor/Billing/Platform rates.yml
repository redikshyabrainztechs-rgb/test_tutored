info:
  name: Platform rates
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/public/platform-rates"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
