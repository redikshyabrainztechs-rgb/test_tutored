info:
  name: Instructor stats for billing
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/instructor/instructor-stats-for-billing"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
