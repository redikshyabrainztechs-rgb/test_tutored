info:
  name: Get Notification
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/users/notification"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accesstoken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
