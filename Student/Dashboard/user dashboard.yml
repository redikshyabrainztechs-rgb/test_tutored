info:
  name: user dashboard
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/student/dashboard"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
