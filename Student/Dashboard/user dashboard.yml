info:
  name: user dashboard
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/student/dashboard"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accesstoken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
