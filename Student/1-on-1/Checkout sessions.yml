info:
  name: Checkout sessions
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/student/checkout-session"
  body:
    type: json
    data: |-
      {
        "product_id"
      :
      "61e661e5-64b5-42ed-94e5-457e2b9c8e02",
      "product_type"
      :
      "1on1"
      }
  auth:
    type: bearer
    token: "{{accesstoken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
