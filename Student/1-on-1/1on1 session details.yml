info:
  name: 1on1 session details
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/student/list/session-bookings"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accesstoken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
