info:
  name: Update session basic
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/instructor/update-basic-session"
  body:
    type: json
    data: |-
      {
        "duration":60,
       "price":300,
       "currency":"NPR",
       "description":"always on time"
      }
  auth:
    type: bearer
    token: "{{baseUrl}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
