info:
  name: Enroll group sessions
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/student/enroll"
  body:
    type: json
    data: |
      {
        "transaction_id" : "b69e9951-6881-4d72-a3fa-d0cd76efa50d",
        "payment_status" : "COMPLETE"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
