info:
  name: Enrolled group live sessions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/student/list/enrolled-live-sessions"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accesstoken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
