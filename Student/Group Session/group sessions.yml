info:
  name: group sessions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/student/enrolled/live-sessions"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
