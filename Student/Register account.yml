info:
  name: Register account
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email":"smirti.brainztechs+3@gmail.com",
      "firstname":"tina",
      "lastname":"Sija",
      "password":"Smirtibam123@",
      "phone_number":"+9779761207415",
      "roletype":"ROLE_STUDENT"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
